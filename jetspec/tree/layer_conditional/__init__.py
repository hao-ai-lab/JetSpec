"""layer_conditional — tree shape from mid-tree drafter refresh (PLANNED).

Placeholder only. Documents mechanism + dependency; intentionally NOT registered
(not imported by ``jetspec.tree``), so the live registry stays at the shipped set.
See ``jetspec/tree/ROADMAP.md``.
"""
